{
  "watch": ["src/**/*.ts", ".env"],
  "ext": "ts,js,.env,json",
  "ignore": ["dist/*", "node_modules/*"],
  "exec": "ts-node -r tsconfig-paths/register  --transpile-only src/app.ts",
  "legacyWatch": true
}
